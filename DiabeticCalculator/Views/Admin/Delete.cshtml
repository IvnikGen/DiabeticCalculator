
@using (Html.BeginForm())
{
    <h4>Вы уверены, что хотите удалить аккаунт @Html.Raw(ViewBag.Login)?</h4>
    <div class="row">
        <div class="col-xs-12 ">
            <button class="btn btn-primary  btn-lg btn-group btn-align-center" type="submit">
                Да
            </button>
        </div>
    </div>
}